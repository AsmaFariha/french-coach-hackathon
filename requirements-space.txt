# Extra dependencies for the HF Space (ZeroGPU backend).
# The Space's requirements.txt should include this file's contents
# in addition to requirements.txt.
#
# NOT needed for local Docker dev — transformers/torch would bloat
# the image significantly and the huggingface_inference backend
# doesn't require them.

transformers>=4.40
accelerate>=0.30
torch>=2.2
